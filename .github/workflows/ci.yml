name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Run tests and upload coverage
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-

      # - name: Locate or install Chrome/Chromium
      #   run: |
      #     # Try to locate an existing Chrome/Chromium binary on the runner
      #     if command -v google-chrome-stable >/dev/null 2>&1; then
      #       echo "CHROME_BIN=$(command -v google-chrome-stable)" >> $GITHUB_ENV
      #     elif command -v google-chrome >/dev/null 2>&1; then
      #       echo "CHROME_BIN=$(command -v google-chrome)" >> $GITHUB_ENV
      #     elif command -v chromium-browser >/dev/null 2>&1; then
      #       echo "CHROME_BIN=$(command -v chromium-browser)" >> $GITHUB_ENV
      #     elif command -v chromium >/dev/null 2>&1; then
      #       echo "CHROME_BIN=$(command -v chromium)" >> $GITHUB_ENV
      #     else
      #       echo "No Chrome/Chromium binary found; installing minimal dependencies"
      #       sudo apt-get update
      #       # libgconf-2-4 is not available on newer Ubuntu images; install modern deps only
      #       sudo apt-get install -y chromium libnss3 libatk1.0-0 libatk-bridge2.0-0 libgbm1 libasound2 libxss1 fonts-liberation
      #       # Attempt to set CHROME_BIN to the installed binary path
      #       if command -v chromium >/dev/null 2>&1; then
      #         echo "CHROME_BIN=$(command -v chromium)" >> $GITHUB_ENV
      #       elif command -v chromium-browser >/dev/null 2>&1; then
      #         echo "CHROME_BIN=$(command -v chromium-browser)" >> $GITHUB_ENV
      #       fi
      #     fi

      - name: Install dependencies
        run: npm ci

      - name: Run tests (headless)
        env:
          CHROME_BIN: /usr/bin/chromium-browser
        run: npm run test:ci

      - name: Upload coverage artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage

  build:
    name: Build production
    runs-on: ubuntu-latest
    needs: test
    strategy:
      matrix:
        node-version: [18.x]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm ci

      - name: Build (production)
        run: npm run build:prod

      - name: Upload build dist
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
